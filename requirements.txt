# requirements.txt - Astro Runtime 3.1-4 (Airflow 3.1.2 - Latest!)
# Updated for latest Airflow 3.x - dbt installed in venv via Dockerfile

# Airflow Providers - Latest versions for Airflow 3.x
apache-airflow-providers-postgres>=6.0.0
apache-airflow-providers-common-sql>=1.20.0

# Astronomer Cosmos - Using pre-release compatible with Airflow 3.1.2
# Note: dbt is installed separately in dbt_venv (see Dockerfile)
astronomer-cosmos[dbt-postgres]==1.12.0a2

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# API & utilities
requests>=2.32.0
psycopg2-binary>=2.9.9

# Additional utilities for Airflow 3.x
pendulum>=3.0.0
